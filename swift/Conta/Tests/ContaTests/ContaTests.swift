import XCTest
@testable import Conta

final class ContaTests: XCTestCase {
    func testSaldo() {
        XCTAssertEqual(0, 0)
    }

    static var allTests = [
        ("testSaldo", testSaldo),
    ]
}
